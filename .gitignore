node_modules/
.DS_Store
*.log
functions/node_modules/
functions/package-lock.json

# Android build outputs
android/app/build/
android/.gradle/
android/capacitor-cordova-android-plugins/build/
*.apk
*.aab
local.properties

# Capacitor build mirror — regenerated by CI (rsync root -> www/), never edited
www/

# Rules-test deps (installed on demand)
test/node_modules/

# Cloud Functions secrets for the emulator (XWEATHER_CLIENT_ID/_SECRET).
# The real values live in Secret Manager; this file is the local stand-in and
# must never be committed — the repo is what the public frontend is built from.
functions/.secret.local
